#include <bits/stdc++.h>
using namespace std;

#define ll long long

void solve(ll n, ll k, ll x) {
    
}

int main() {
ios_base::sync_with_stdio(false);
cin.tie(NULL);
    int t = 1;
    cin >> t;
    while(t--) {
        ll n, k, x;
        solve(n, k, x);
    }
    return 0;
}